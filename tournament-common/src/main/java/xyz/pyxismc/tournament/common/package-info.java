/**
 * Objects shared between the Velocity proxy and the Paper servers:
 * models, DTOs, enums, events, Redis protocol and match results.
 * <p>
 * This module is shaded into both plugins.
 */
package xyz.pyxismc.tournament.common;

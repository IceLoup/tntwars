package xyz.pyxismc.tournament.common.enums;

/**
 * Role of a player inside a team.
 */
public enum TeamRole {

    /** Team creator and leader. Only the captain can manage the team. */
    CAPTAIN,

    /** Regular team member. */
    MEMBER
}
